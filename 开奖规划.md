# 开奖规划（两阶段：封盘预生成 → 开奖时间结算）

> 目标：**封盘后先确定开奖号码并写入（不触发结算/分成）**；到达开奖时间后，再执行“公布开奖号码 + 结算派彩（代理分成后续规划）”等动作。  
> 说明：本文件仅为规划，按你的要求**暂不修改任何代码**。

## 1. 现状梳理（基于当前代码）

### 1.1 前端（`E:\Code\Php\bc\vue\home`）

- 开奖页面路由：`vue/home/src/router/index.js` 中 `/kj` → `vue/home/src/views/Kj.vue`
- 页面行为（`vue/home/src/views/Kj.vue`）：
  - 先调 `GET /api/lottery_bet/getCurrentQishu` 获取 `qishu` 和 `draw_time`
  - 再轮询 `GET /api/lottery_bet/getKjResult` 获取开奖号码 `numbers`
  - **只要接口返回了 `numbers`（>=7 个）就会立即展示并播放开奖动画**，并缓存到 `localStorage`

结论：如果后端在封盘后就把号码写进当前接口读取的字段里，前端会提前展示开奖号码（除非后端做“未到开奖时间不返回号码”的保护）。

### 1.2 后端公开接口（`E:\Code\Php\bc\server`）

#### 1) 获取当前期号
- `server/app/api/controller/LotteryBetController.php`：`getCurrentQishu()`
  - 查询 `la_lottery_issue` 返回 `issue/open_time/close_time/draw_time/status/plate_code`
  - 前端开奖页（KJ）用它拿当前 `qishu`

#### 2) 获取开奖结果（KJ 页面轮询的接口）
- `server/app/api/logic/LotteryBetLogic.php`：`getKjResult($gid,$qishu,$plateCode)`
  - 直接读取 `la_lottery_issue.result` 拆成 `numbers`
  - **当前不强制校验 `draw_time` 或期号状态**，只要 `result` 有值就会返回号码（并置 `status=1`）

结论：想“封盘先写入但不提前展示”，需要：
- 要么 **封盘阶段不要写入 `la_lottery_issue.result`**（改写入新的“预生成字段/表”）
- 要么 改 `getKjResult`：**未到 `draw_time` 一律不返回号码**

### 1.3 后端后台接口（你提到的“后台点用此方案开奖”）

- `server/app/adminapi/controller/BestPlanController.php`：`executeDrawing()`
- 实际逻辑在：`server/app/api/logic/BestPlanLogic.php`：`executeDrawing(...)`
  - 当前行为：**立即写入 `la_lottery_issue.result` + 立即遍历订单做结算/派彩/流水（同一次调用完成）**

结论：要实现两阶段，需要把该接口的语义调整为：
- **“提交/写入预生成开奖号码”**（不结算）
- 结算改为由**开奖时间的任务**统一执行

### 1.4 定时任务/命令（可承载“开奖时间结算”）

- `server/app/common/command/Crontab.php`：系统内置定时任务调度器（从表里读取任务并 `Console::call`）
- `server/app/common/command/DrawLottery.php`：已有开奖命令雏形
  - 会扫描待开奖期次（`LotteryIssue::getPendingDrawIssues()`）
  - 设置 `issue->result` 并做结算（冻结解冻、中奖流水、代理佣金等）

备注：当前 `DrawLottery.php` 里还存在“随机生成号码”的逻辑；后续可替换为“读取预生成号码”或“没预生成则兜底生成”。

### 1.5 数据表现状（从仓库 SQL 规划文件观察）

在 `server/database/migrations/lottery_new_tables.sql` 中，`la_lottery_issue` 规划字段包含：
- `result`、`status`、`is_settled`、`settled_at`、`total_bet_amount`、`total_prize_amount`
- `status` 注释为：`1=待开盘 2=投注中 3=已封盘 4=已开奖 5=已结算`

但当前业务代码里对 `status` 的使用存在多套口径（部分代码用 0/1/2/3，部分代码用 1/2/3/4/5）。  
**落地前必须以 newbc 实际表/数据为准统一口径。**

---

## 2. 目标流程（两阶段）

### 2.1 两阶段时序（推荐）

1. **封盘后（close_time 到达）**
   - 后端基于本期下注情况计算“计划开奖号码”
   - 写入数据库（**但不触发派奖、流水、分成**）
   - 允许后台人员查看/覆盖（可选）

2. **到开奖时间（draw_time 到达）**
   - 将“计划开奖号码”作为本期最终开奖号码对外公布
   - 执行结算：判奖、解冻、派奖、生成中奖记录、写流水（代理分成后续实现）
   - 标记期次已结算，并触发创建下一期（如现有逻辑要求）

### 2.2 关键约束

- **对外接口在 `draw_time` 之前不能泄露号码**（否则新 `/kj` 页面会提前开奖）
- 结算必须**幂等**（任务重复执行不应重复派彩；代理分成后续实现也必须幂等）
- 支持多盘（plate_code A/B/C），每盘独立封盘、开奖、结算

---

## 3. 方案设计（推荐：新增“预生成结果”字段/表）

### 3.1 数据设计（推荐做法）

在 `la_lottery_issue` 增加“预生成开奖号码”字段（或单独建表，二选一）：

**方案 A（推荐，改动最小、对外不泄露）：在 `la_lottery_issue` 加字段**
- `planned_result`：VARCHAR(255) 默认空，存 `m1,...,m7`
- `planned_at`：INT，预生成时间
- `planned_source`：TINYINT（0=auto 1=admin）
- `planned_operator_id`：INT（后台操作员/系统）
- 可选：`plan_meta`（TEXT/JSON，存算法版本、利润率摘要等）

对外公布仍使用原字段：
- `result`：仅在 `draw_time` 到达并“正式开奖”时写入

**为什么推荐方案 A：**
- 前端 `getKjResult` 当前读取的是 `result`，只要我们封盘阶段不写 `result`，就不会提前展示
- 风险低：不需要立刻改动所有查询 `result` 的代码路径

**方案 B（不加字段，复用 `result`，靠接口/状态挡住）：**
- 封盘阶段直接写 `result`，但 `getKjResult`/`getResultList` 必须判断 `now >= draw_time` 或 `status>=已开奖` 才返回号码
- 缺点：需要改所有读取 `result` 的对外出口，否则容易“提前泄露”

### 3.2 状态机规划（以 DB 实际为准，建议统一）

建议统一 `la_lottery_issue.status` 生命周期（示例，和 `lottery_new_tables.sql` 对齐）：
- `1` 待开盘
- `2` 投注中
- `3` 已封盘（此时允许写入 `planned_result`）
- `4` 已开奖（此时写入 `result`，对外可见）
- `5` 已结算（结算完成，`is_settled=1`）

> 如果你现网使用的是 0/1/2/3 口径，需要先做一次“状态口径统一/迁移”再落地两阶段流程。

### 3.3 后端任务拆分

推荐用一个“每分钟执行一次”的任务同时处理两段逻辑：

1) **封盘检查 + 预生成**
- 条件：`now >= close_time` 且期次未封盘（或处于“投注中”）
- 动作：
  - 更新期次状态为“已封盘”
  - 若 `planned_result` 为空：调用最佳方案算法生成号码并写入 `planned_result`

2) **到点开奖 + 结算**
- 条件：`now >= draw_time` 且 `result` 为空 且未结算
- 动作：
  - `result = planned_result`（若 planned 为空则兜底生成或实时计算一次）
  - 更新状态为“已开奖”
  - 执行结算（判奖、解冻、派奖、流水；代理分成后续实现）
  - 标记 `is_settled=1`、`settled_at`、状态“已结算”
  - 触发创建下一期（如你现有流程依赖）

承载方式：
- 复用 `server/app/common/command/DrawLottery.php`（把“随机生成”改成“读取 planned_result/兜底生成”，并加上封盘预生成逻辑）
- 或新增命令 `plan:lottery` + `draw:lottery`，再通过 `Crontab` 系统调度

### 3.4 “后台点用此方案开奖”接口改造建议

现有接口：
- `POST /adminapi/best_plan/executeDrawing`（`BestPlanController::executeDrawing` → `BestPlanLogic::executeDrawing`）

建议改造为：
- **仍保留原路由（兼容前端后台），但语义改为“提交预生成号码”**
  - 校验期次必须已封盘（或至少 `now >= close_time`）
  - 写入 `planned_result/planned_at/planned_source=admin`
  - 不做任何结算/派彩/分成

如需要“手动立即结算/补结算”，建议新增后台接口：
- `POST /adminapi/best_plan/publishAndSettle`（强制把 planned_result 发布到 result 并结算）
  - 仅管理员可用
  - 必须做幂等保护（防重复派彩）

### 3.5 公开接口（KJ 页）兼容策略

`GET /api/lottery_bet/getKjResult`：
- 推荐保持返回结构不变
- 只要我们**不在封盘阶段写 `result`**，KJ 页无需改动即可实现“到点才出结果”
- 可选增强：增加字段 `plan_ready`（仅后台/内部使用），**不要给未登录用户返回**，避免信息泄露

---

## 4. 数据库变更规划（以 MySQL 5.7 为准）

> 你给的 MySQL：`D:\phpstudy_pro\Extensions\MySQL5.7.26\bin`，DB：`newbc`，账号：`root/root`。

### 4.1 推荐 SQL（方案 A：加字段）

```sql
ALTER TABLE la_lottery_issue
  ADD COLUMN planned_result VARCHAR(255) NOT NULL DEFAULT '' COMMENT '封盘后预生成开奖号码(未公开)',
  ADD COLUMN planned_at INT(10) UNSIGNED NOT NULL DEFAULT 0 COMMENT '预生成时间',
  ADD COLUMN planned_source TINYINT(1) UNSIGNED NOT NULL DEFAULT 0 COMMENT '0=auto,1=admin',
  ADD COLUMN planned_operator_id INT(11) UNSIGNED NOT NULL DEFAULT 0 COMMENT '预生成操作员ID(0=系统)';

-- 可选：加索引，便于任务扫描
CREATE INDEX idx_issue_plan ON la_lottery_issue (game_id, plate_code, status, close_time);
CREATE INDEX idx_issue_draw ON la_lottery_issue (game_id, plate_code, status, draw_time);
```

### 4.2 若你希望“强审计/可追溯”（备选：单独建表）

单独建 `la_lottery_issue_plan`（记录每次计划生成/覆盖的历史），字段建议：
- `issue_id / game_id / plate_code / issue`
- `planned_result`
- `meta`（TEXT/JSON）
- `created_at`
- `created_by`（admin_id/0=system）
- `status`（active/overridden）

---

## 5. 实施步骤（建议顺序）

1. **确认现网 status 口径**
   - 用 SQL 查询 `la_lottery_issue` 最近几期，观察 status 随时间变化
   - 确认“投注中/封盘/开奖/结算”各自对应的状态值

2. **做 DB 变更（加 planned 字段或建表）**

3. **后端改造（核心）**
   - 把“生成号码”与“结算派彩/分成”拆成两个阶段
   - 改后台接口：`executeDrawing` 只写 planned，不结算
   - 改定时任务：到 `draw_time` 才发布 result 并结算
   - 幂等与锁：基于 `is_settled`、`result`、行锁（`SELECT ... FOR UPDATE`）确保不会重复结算

4. **前端验证**
   - `Kj.vue` 在封盘后应只显示倒计时，不应提前出号
   - 到达开奖时间后，出号并播放动画

5. **回归测试要点**
   - 封盘后不允许再下注
   - 结算金额、冻结解冻、流水只发生一次（代理分成后续实现也必须幂等）
   - 多盘 A/B/C 互不影响

---

## 6. 已确认的关键约束（按你最新回答，作为最终落地细节）

1. **封盘后“预生成号码”是否允许普通用户通过任何接口看到？**
   - 结论：**不允许**（仅后台可见），
     否则新 `/kj` 页面会提前开奖。
   - 落地要求：
     - 公开接口（如 `getKjResult`）只读取 `result`，
       并确保 `draw_time` 前 `result` 一直为空；
       或强制校验 `now >= draw_time` 才返回。
     - `planned_result` 仅后台接口可读，
       后台接口必须鉴权，并记录操作日志（见 3.1/3.4）。

2. **后台“用此方案开奖”按钮的期望行为：**
   - 结论：**封盘后提交计划**（写入 `planned_result`，
     不触发结算/派彩/分成）。
   - 如需“手动立即结算/补结算”，建议另开独立后台接口 `publishAndSettle`，
     并必须做幂等保护。

3. **结算与代理分成规则：**
   - 当前：**暂不实现代理分成**（本轮只完成“计划出号”与
     “到点结算”的拆分）。
   - 预留规划：后续按“下注金额百分比”计算佣金，例如：
     `commission = bet_amount * agent_rate`
     建议在结算阶段统一生成佣金记录，
     并与 `is_settled` 一起做幂等保护。

4. **多盘的开奖时间与封盘时间来源：**
   - 结论：以 `la_lottery_issue.close_time/draw_time` 为准，
     各盘各期独立生效。

---

## 7. 代理分成（后续规划：按下注金额百分比）

> 本轮不实现，仅做结构预留，避免将来重构“结算幂等/流水/余额”。

### 7.1 计算口径（建议）

- **计算基数**：以注单/订单明细的 `bet_amount` 为基数；
  撤单/退款/无效单不计（口径后续再细化）。
- **计算公式**：`commission_amount = ROUND(bet_amount * agent_rate, 2)`
  （精度与舍入规则后续统一确认）。
- **触发时机**：统一在 `draw_time` 的“结算阶段”执行
  （与派奖同一个事务/同一个幂等锁）。

### 7.2 数据记录（建议新增表，或复用现有佣金表）

- 表名建议：`la_agent_commission`
- 建议字段：
  - `issue_id / game_id / plate_code / issue`
  - `bet_id`（或 `order_id` / `order_item_id`，视你现有表结构）
  - `user_id`（下注人）
  - `agent_user_id`（拿佣金的代理）
  - `level`（代理层级，可选）
  - `bet_amount / agent_rate / commission_amount`
  - `status`（0=待入账 1=已入账 2=作废）
  - `created_at`
- **幂等建议**：对 `(issue_id, bet_id, agent_user_id)` 建唯一键；
  结算时先查/先写再入账，防止重复。

### 7.3 入账与流水（建议）

- 入账：代理余额增加 `commission_amount`
- 流水：写入与你现有“中奖/解冻/充值”同风格的账变流水，
  便于对账与回滚

